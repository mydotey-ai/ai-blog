# AI 博客系统

> 一个基于 Vue 3 + Spring Boot 的现代化博客系统，采用克莱因蓝极简主义设计风格。

[![Java](https://img.shields.io/badge/Java-21-orange.svg)](https://openjdk.java.net/)
[![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.5.0-brightgreen.svg)](https://spring.io/projects/spring-boot)
[![Vue](https://img.shields.io/badge/Vue-3.5-4FC08D.svg)](https://vuejs.org/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.7-blue.svg)](https://www.typescriptlang.org/)
[![License](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

## 📚 目录

- [技术栈](#技术栈)
- [功能特性](#功能特性)
- [快速开始](#快速开始) ⭐
- [手动启动](#手动启动开发模式)
- [项目结构](#项目结构)
- [API 端点](#api-端点)
- [数据库结构](#数据库结构)
- [开发指南](#开发指南)
- [故障排查](#故障排查)

## ✨ 项目亮点

- 🚀 **一键启动** - 零配置，运行 `./start.sh` 即可
- 🎨 **克莱因蓝设计** - 独特的极简主义视觉风格
- 🔒 **JWT 认证** - 无状态、安全的用户认证
- 📱 **响应式设计** - 完美适配桌面和移动端
- ⚡ **现代技术栈** - Vue 3 + Spring Boot 3.5
- 📝 **Markdown 支持** - 优雅的文章编写和渲染

## 技术栈

### 后端
- Java 21
- Spring Boot 3.5.0
- Spring Security + JWT 认证
- Spring Data JPA
- PostgreSQL 16
- Gradle

### 前端
- Vue 3.5
- TypeScript 5.7
- Vite 6.0
- Tailwind CSS 3.4
- Vue Router 4.5
- Pinia 2.3
- Axios

## 功能特性

### 公开功能
- 📝 博客文章浏览（支持 Markdown）
- 🏷️ 标签分类
- 💬 匿名评论系统
- 🔍 文章搜索

### 管理功能
- 🔐 管理员登录认证
- ✍️ 文章 CRUD（支持草稿/发布）
- ✅ 评论审核
- 🎨 标签管理

## 快速开始

### 前置要求

1. **Java 21**
   ```bash
   java -version  # 应显示 Java 21
   ```

2. **Node.js 18+**
   ```bash
   node -v  # 应显示 v18.x 或更高
   ```

3. **PostgreSQL 16**
   ```bash
   psql --version  # 应显示 PostgreSQL 16.x
   ```

   确保 PostgreSQL 服务正在运行，并且有以下凭据：
   - 用户名: `postgres`
   - 密码: `xx123456XX`

### 一键启动

```bash
# 启动所有服务（数据库初始化、后端、前端）
./start.sh
```

脚本会自动完成以下操作：
1. 检查 PostgreSQL 服务
2. 创建数据库（如果不存在）
3. 更新后端配置
4. 创建默认管理员账号
5. 启动后端服务（端口 8080）
6. 启动前端开发服务器（端口 3000）

### 访问系统

启动成功后：

- **前端首页**: http://localhost:3000
- **管理后台**: http://localhost:3000/admin
- **后端 API**: http://localhost:8080

**默认管理员账号**：
- 用户名: `admin`
- 密码: `admin123`

⚠️ **重要**: 首次登录后请立即修改密码！

### 停止服务

```bash
./stop.sh
```

### 查看日志

```bash
# 后端日志
tail -f backend.log

# 前端日志
tail -f frontend.log
```

## 手动启动（开发模式）

如果你想分别启动各个服务：

### 1. 初始化数据库

```bash
# 执行数据库初始化脚本
PGPASSWORD=xx123456XX psql -h localhost -U postgres -f scripts/init-db.sql
```

### 2. 启动后端

```bash
cd backend
./gradlew bootRun
```

后端将在 http://localhost:8080 启动

### 3. 启动前端

```bash
cd frontend
npm install  # 首次运行
npm run dev
```

前端将在 http://localhost:3000 启动

## 项目结构

```
ai-blog/
├── backend/                 # Spring Boot 后端
│   ├── src/main/java/com/mydotey/blog/
│   │   ├── config/         # 配置类
│   │   ├── controller/     # REST 控制器
│   │   ├── dto/            # 数据传输对象
│   │   ├── entity/         # JPA 实体
│   │   ├── filter/         # 过滤器
│   │   ├── repository/     # 数据访问层
│   │   ├── service/        # 业务逻辑层
│   │   └── util/           # 工具类
│   └── src/main/resources/
│       └── application.yml # 应用配置
├── frontend/               # Vue 3 前端
│   ├── src/
│   │   ├── assets/         # 静态资源
│   │   ├── components/     # Vue 组件
│   │   ├── router/         # 路由配置
│   │   ├── services/       # API 服务
│   │   └── views/          # 页面组件
│   ├── index.html          # HTML 入口
│   ├── vite.config.ts      # Vite 配置
│   └── tailwind.config.js  # Tailwind 配置
├── docs/                   # 文档
│   ├── plans/              # 实施计划
│   └── ui-design/          # UI 设计文档
├── scripts/                # 脚本
│   └── init-db.sql         # 数据库初始化
├── start.sh                # 一键启动脚本
└── stop.sh                 # 停止服务脚本
```

## API 端点

### 公开 API

- `GET /api/posts` - 获取文章列表（支持分页、标签过滤、搜索）
- `GET /api/posts/{slug}` - 获取文章详情
- `GET /api/tags` - 获取所有标签
- `GET /api/comments?postId={id}` - 获取文章评论
- `POST /api/comments` - 发表评论
- `POST /api/auth/login` - 管理员登录

### 管理员 API（需要认证）

- `GET /api/admin/posts` - 获取所有文章（含草稿）
- `POST /api/admin/posts` - 创建文章
- `PUT /api/admin/posts/{id}` - 更新文章
- `DELETE /api/admin/posts/{id}` - 删除文章
- `GET /api/admin/comments` - 获取所有评论
- `PUT /api/admin/comments/{id}/status` - 更新评论状态
- `DELETE /api/admin/comments/{id}` - 删除评论

## 数据库结构

系统使用以下 5 个主要表：

- `admins` - 管理员账号
- `posts` - 博客文章
- `tags` - 标签
- `post_tags` - 文章标签关联（多对多）
- `comments` - 评论

详细结构请参考 `scripts/init-db.sql`

## 开发指南

### 前端开发

```bash
cd frontend
npm run dev      # 开发模式
npm run build    # 生产构建
npm run preview  # 预览构建结果
```

### 后端开发

```bash
cd backend
./gradlew bootRun        # 运行应用
./gradlew build          # 构建 JAR
./gradlew clean build    # 清理并构建
```

## 配置说明

### 后端配置 (`backend/src/main/resources/application.yml`)

主要配置项：
- 数据库连接信息
- JWT 密钥和过期时间
- 服务器端口
- 日志级别

### 前端配置

- `vite.config.ts` - Vite 构建配置
- `tailwind.config.js` - Tailwind CSS 主题配置
- `tsconfig.json` - TypeScript 编译配置

## 设计系统

项目采用**克莱因蓝极简主义**设计风格：

- **主色调**: 克莱因蓝 (#0022FF)
- **字体**: Space Grotesk (正文), Monument Extended (标题)
- **布局**: 超大留白，精致排版
- **动画**: 微妙的过渡效果

详细设计规范请参考 `docs/ui-design/design-system.md`

## 故障排查

### 后端无法启动

1. 检查 PostgreSQL 是否运行: `pg_isready`
2. 检查数据库是否创建: `psql -U postgres -l | grep aiblog`
3. 检查端口 8080 是否被占用: `lsof -i:8080`
4. 查看后端日志: `tail -f backend.log`

### 前端无法启动

1. 检查 Node.js 版本: `node -v`
2. 重新安装依赖: `cd frontend && rm -rf node_modules && npm install`
3. 检查端口 3000 是否被占用: `lsof -i:3000`
4. 查看前端日志: `tail -f frontend.log`

### 数据库连接失败

1. 检查 PostgreSQL 服务状态
2. 验证用户名密码是否正确
3. 检查 `application.yml` 中的数据库配置
4. 确认数据库 `aiblog` 已创建

## 待办功能

- [ ] Markdown 编辑器集成（md-editor-v3）
- [ ] 图片上传功能
- [ ] 文章搜索优化
- [ ] 评论验证码
- [ ] 邮件通知
- [ ] RSS 订阅
- [ ] 文章归档
- [ ] 访问统计

## 许可证

MIT License

## 贡献者

- 系统设计与实现: Claude Sonnet 4.5
- 项目创建日期: 2026-02-01

---

**提示**: 本项目仅用于学习和演示目的，生产环境使用前请进行安全加固（修改默认密码、配置 HTTPS、启用速率限制等）。
